# запускается от root (UID 0)
apiVersion: v1
kind: Pod
metadata:
  name: pod-root-user
  namespace: gatekeeper-check
spec:
  securityContext:
    runAsUser: 0
    runAsGroup: 0
  containers:
    - name: nginx
      image: nginx:latest
      securityContext:
        allowPrivilegeEscalation: true